

<div class="right_align">
    <%= link_to("create",new_admin_workshop_path,{:class=>"popup"})%>
</div>
<h3>车间列表</h3>
<%=notice%>
<%= table_for(@workshops) {|item,col|
    @workshop = item
    col.add "名称",in_place_editor_field(:workshop,:name)
    col.add "负责人",item.user.login #in_place_editor_field(:material,:sub_class,{},{:collection=>ListItem.inplace_select_list_of("sub_class")}
    col.add "设备", link_to("#{item.equipments.size} 台",admin_equipments_path(:workshop_id=>item.id))
    col.add :action,  link_to("delete",admin_workshop_path(item),{:method=>:delete})  
}%>